{
  "rules": {
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    },
    "points": {
      "$uid": {
        ".read": "$uid === auth.uid"
      }
    }
  }
}